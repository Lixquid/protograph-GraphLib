/// Detects if the current point is in water; fires should be extinguished and
/// objects should become wet if this is true.
module SampleWater

in Position: float3

out this: bool

where SampleValueSpatialVariable<bool>(
	Point = Position,
	Name = "Water",
)
